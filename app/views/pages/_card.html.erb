<%# <div class="modal fade" id="cardModal" tabindex="-1" aria-hidden="true"> %>
  <div class="modal-dialog">
    <div class="modal-content">
      <%= simple_form_for [container, card],
          method: :patch do |f| %>
        <div class="d-flex flex-column">
          <%= f.input :title, label: "Card Title", required: false, placeholder: card.title %>
          <%= f.input :description,
              as: :text, :input_html => { :cols => 5, :rows => 8 },
              label: "Description", placeholder: card.description %>

          <%= f.submit :submit, label: "Update Card" ,class: "btn btn-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
<%# </div> %>
